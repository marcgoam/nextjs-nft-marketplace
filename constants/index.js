const networkMapping = require("./networkMapping.json")
const marketplaceAbi = require("./NftMarketplace.json")
const nftAbi = require("./Raccoons.json")

module.exports = {
    networkMapping,
    marketplaceAbi,
    nftAbi,
}
